name: mysakhi_app
description: MySakhi Emergency Safety App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_database: ^11.1.4
  firebase_storage: ^12.3.3
  provider: ^6.1.2
  permission_handler: ^11.3.1
  url_launcher: ^6.3.1
  intl: ^0.19.0
  uuid: ^4.3.3
  record: ^5.1.0
  audioplayers: ^6.0.0
  geolocator: ^12.0.0
  flutter_blue_plus: ^1.31.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true